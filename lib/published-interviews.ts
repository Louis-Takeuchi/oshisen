import type { InterviewDocument } from "./interviews.ts";

/** Server-page input only. Never import this registry from a client component. */
export const publishedInterviewDocument: InterviewDocument = {
  schemaVersion: "1.0",
  blocks: [],
};
