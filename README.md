# オシセン

2026年 茨城県議会議員選挙・**つくば市選挙区**を対象に準備する Phase 0 のプロトタイプです。
政策の回答を一問ずつ見比べる部分と、本人が語った経験・選択・判断理由・条件を読む部分を分け、元の発言へつなぎます。

## 現在の方針と掲載状況

- **候補者情報は0件。Podcast取材・本人回答の取得はこれからです。** 仮名候補者や架空の政策回答は公開画面に載せません。
- 政策回答は一問ずつの照合に変更し、総合一致率、候補者の近さの順位、性格の点数にはまとめません。
- P（政策への回答）・H（共通インタビュー）・N（利用者の知りたいこと）を別の情報として扱います。H/Nを人物評価や加点に使いません。
- 研究の中心は、同じ取材情報を取材順に示すB条件と、共通項目で示すC条件を比べ、発言内容と根拠を把握しやすくなるかを確かめることです。効果は未検証です。
- 現在の8問は草案です。制度・権限・基準時点・用語・出典・認知インタビューを確認してから本番の版を確定します。
- [研究対応設計](docs/research-design.md) が2026-09-18以降の実装方針です。[要求定義v1](docs/requirements-v1.md) は当初の記録として残し、総合一致率や仮名4名等の旧仕様より新方針を優先します。

## 現在の画面

| パス                                   | 内容                                                                  |
| -------------------------------------- | --------------------------------------------------------------------- |
| `/`                                    | サービスの入口、対象地域と掲載準備状況                                |
| `/diagnosis`・`/questions`・`/results` | 政策質問への回答・見直し。一問ごとの照合の入口                        |
| `/candidates`・`/candidates/[id]`      | 候補者情報の掲載先。現在は候補者0件                                   |
| `/compare`・`/saved`                   | 2人の比較、気になる候補のブラウザ内保存。候補者掲載前は空の状態を案内 |
| `/issues`                              | 政策の問いをテーマから読む                                            |
| `/interests`                           | 最大3テーマと知りたい情報の種類から、見る場所を選ぶ                   |
| `/stories`                             | 診断不要の入口。共通インタビュー6問と取材前の状況                     |
| `/research`                            | 質問台帳・取材ガイド・研究の準備、手元の情報ブロックの表示確認        |
| `/policy-register`                     | 設問候補の台帳、草案・保留の区別と公開前の確認事項                    |
| `/about`                               | 企画、運営者の写真・本人回答、公式SNS・問い合わせ                     |
| `/method`・`/sources`・`/privacy`      | 回答照合・出典・公平性・データの扱い                                  |

手描きのロゴと既存のPOPなデザインを引き継いでいます。地域は `lib/project.ts` にまとめています。
運営メンバーの写真・本人回答と、今後掲載する候補者の情報は別に扱います。

## Vercelで公開する

このリポジトリは **Vercel用のNext.js** と、既存の **Sites用のvinext** の両方に対応しています。
アプリの画面とデータは共通です。Vercelへの公開操作は手動で行ってください。

1. Vercelで `Louis-Takeuchi/oshisen` をImportする。
2. Production Branchは `main`、Root Directoryはリポジトリのルート（未指定）にする。
3. Framework Presetは **Next.js**、Node.js Versionは **24.x** を選ぶ。
4. Build Commandは `npm run build:vercel`、Install Commandは `npm ci`。`vercel.json` に設定済みです。
5. Output Directoryは **Next.jsの既定値のまま**。`dist` や `public` に変更しない。
6. Deployする。追加のAPIキー・DB・環境変数は不要です。

独自ドメインが決まったら、必要に応じて環境変数 `SITE_URL=https://公開ドメイン` をProductionだけに設定して再デプロイします。
未設定時の共有画像URLはアクセス先ホストから生成されるため、VercelのPreview URLでも動きます。設定例は `.env.example` にあります。
既存のSitesの閲覧制限はVercelには引き継がれません。Vercel側の公開範囲・Deployment Protectionを確認してください。

```sh
npm ci
npm run dev:vercel
# 本番相当のビルド・配信チェック（公開操作は行いません）
npm run build:vercel
npm run test:vercel
# 本番相当のサーバーを自分で開く場合
npm run start:vercel
```

ローカルとVercelのビルドを揃えるため、Next.js標準のWebpackビルドを明示しています。
VercelにCloudflare Workerの `worker/index.ts` や `dist/` を指定する必要はありません。
設定の根拠：[VercelのNext.js対応](https://vercel.com/docs/frameworks/full-stack/nextjs)、[vercel.jsonの設定](https://vercel.com/docs/project-configuration/vercel-json)。

### 訪問計測を有効にする

`@vercel/analytics` を組み込み済みです。既存サイトを作り直したり、APIキーやデータベースを追加したりする必要はありません。
すでにサイトを公開していても、計測コードを反映した新しいデプロイが必要です。公開操作は手動で行ってください。

1. 既存のVercelプロジェクトの **Analytics / Web Analytics** を開き、有効化されていることを確認する。
2. この変更を含むコードをProductionにデプロイする。PreviewやSites、ローカルでは集計しません。
3. シークレットウィンドウ等で公開サイトのトップURLを直接開き、数ページ移動する。DNT/GPCが有効なブラウザでは集計されません。
4. 約30秒待ち、VercelのAnalyticsでProductionの訪問数・閲覧数を確認する。表示されない場合は停止設定、広告ブロック、対象の環境・期間、Vercel側の有効化状態を確認する。

有効化条件はサーバー側の `NODE_ENV === "production"` かつ `VERCEL_ENV === "production"` です。
`VERCEL_ENV` はVercelのシステム環境変数を使います。通常は手入力不要ですが、未設定の場合はプロジェクトの環境変数設定にある **Automatically expose System Environment Variables** を確認して再デプロイしてください。
初期設定では集計が有効ですが、`/privacy#visitor-analytics` からこのブラウザで停止できます。
実際にデータが届いたかの確認は、更新後の本番デプロイで行います。
公式手順：[Vercel Web Analyticsの導入](https://vercel.com/docs/analytics/quickstart)。

## Sitesでの開発と共通検証

Node.js 22.13以降が必要です。

```sh
npm ci
npm run dev
npm run lint
npm run typecheck
npm run build
npm test
```

`npm test` のルート検査は直前の `dist/` を使います。コード変更後は先にビルドしてください。
回答照合・保存・行動記録はNodeのテスト、ページは配信用WorkerへのHTTPリクエストで検証します。
以前の診断フローではブラウザ操作も確認しています。今回の研究対応版は、下記の自動テスト・型検査・両配信環境のビルドで確認します。新しい画面のブラウザ操作確認は別途必要です。
本番だけ画面移動が失敗する問題を防ぐため、ブラウザの起動コードと遅延読み込みする画面移動処理を分離し、生成コードの回帰検査も行います。
Vercel用は `npm run test:vercel` がNext.jsの本番サーバーを一時起動し、全ページ・404・アイコン・共有画像・配信用設定を検査します。
両ビルドが生成するルート型の競合を避けるため、`npm run typecheck` はNext.jsのルート型を再生成してから型検査します。
ファビコン類の再生成は `npm run icons:generate`。元画像は `public/brand-logo.png` で、ユーザー提供のロゴ全体を使います。青い文字・黄色のずらし・ピンクのハートを含む絵柄を切り取らず、白い正方形に縦横比を保って収めます。以前の吹き出しアイコンや別の略字マークは生成しません。
`sharp` で16・32・48px入りのICO、180pxのAppleアイコン、192・512pxのPNGへ変換します。サイズ変換に外部フォント・画像サービス・APIキーは不要です。ロゴを差し替えたら再生成し、`app/layout.tsx` と `public/site.webmanifest` のアイコンURLの版（現在 `logo-2`）も揃えて更新してください。

2026-09-14時点で `npm audit --omit=dev` の実行時依存関係は既知の脆弱性0件です。
既存のSites用ツール等の開発用依存関係には監査警告が残っています。今回、既存配信環境を大きく変更する一括更新は行っていません。

## データと計測

政策への回答と質問の版は、このブラウザの `sessionStorage` に保持します。質問の版が違う回答を自動照合しません。
「賛成でも反対でもない」という中立の回答、判断保留、スキップ、未回答、候補者回答の確認中を区別します。
過去の発言から本人回答を補完せず、一問でも複数問でも、総合点や人物評価にはしません。

比較対象と知りたいテーマはタブ内に、明示的に保存した候補者IDだけは `localStorage` に保存する構成です。端末間同期はありません。
知りたい情報の種類は画面内のみの選択で、再読み込み後には保持しません。
知りたいテーマはページの案内や共通の絞り込みに使い、候補者の加点や優先表示には使いません。
保存できない場合は一時保持を案内し、利用を継続できます。プライバシーページからローカルデータを削除できます。

### 通常利用の任意操作記録

`/privacy#data-controls` で有効にした場合だけ、操作名や質問ID等をタブ内に記録します。政策の回答値は含めず、サーバーや外部解析へ転送しません。
この設定は研究参加への同意ではありません。JSONの書き出しは明示的な操作で行います。
通常の記録はv2・表示条件 `standard` に統一し、旧版の操作ログ・同意・`policy` / `policy-humanity` の切り替えは引き継ぎません。

### 研究の表示確認

`/research` は、手元のJSONを読み込み、構造を検証し、承認済みブロックをB（取材順）/C（共通形式）で表示するための準備画面です。
同じ素材を使い、取材内容を作り足しません。候補者データや取材発言を標準で同梱しません。

- 読み込んだ情報・確認操作の記録は画面のメモリだけに保持し、ブラウザ保存領域にもサーバーにも保存しません。
- 記録はその画面で任意に有効にした場合のみ。文脈展開、原資料へのクリック、経過時間、B/C、自由閲覧/理解課題、再クリックを区別します。
- 固定の `local-check` は動作確認用の識別子で、実参加者への割り付けや研究用ID発行ではありません。
- 記録停止・明示削除・別ファイル読み込みで記録を消します。再読み込みやタブを閉じても消えます。
- 手動ダウンロードしたJSONは利用者の手元に残ります。サイトの削除操作では消せません。
- 研究募集、本番の無作為割付、同意取得、参加者間の集計は未実装です。

### 訪問集計との違い

Vercel Web Analyticsは、通常の任意操作記録・研究表示確認とは別の仕組みです。Vercel本番環境でのみ、訪問数とページ種類ごとの閲覧数を集計します。
Cookieを使わず、接続時の情報から訪問者数やおおまかな地域等を集計します。IPアドレス等が通信で一切処理されないという意味ではありません。

- 候補者詳細の計測URLを `/candidates/[id]` にまとめ、クエリ文字列とフラグメントを除きます。
- 政策回答、候補者ID、保存・比較対象、知りたいテーマ、ローカル操作記録、読み込んだ取材JSONは送りません。
- `Referrer-Policy: origin` を使い、参照元URLが条件を満たさない場合は計測を開始しません。集計が少なめになる場合があります。
- `/privacy#visitor-analytics` から停止できます。DNT/GPCが有効、保存設定が読み取れない場合も停止します。
- ローカルデータ削除では訪問集計の停止設定を残します。すでに送られた集計や配信ログの削除ではありません。
- 通常のページ配信では、実際のリクエストURL等を配信基盤が処理します。保存期間や契約設定は別途確認してください。

詳細：[Vercel Web Analyticsのプライバシー説明](https://vercel.com/docs/analytics/privacy-policy)。

## 本番データへの接続

### 運営メンバーのプロフィール

`lib/team.ts` に表示名・本名・役職・担当・写真・本人回答をまとめています。候補者データや政策回答の照合には含めません。
回答の追加は本人から受け取った文面を `answers` の該当質問IDへ登録します。未回答や空欄は「本人の回答を準備中」と表示し、性格・経歴・趣味などを推測で補いません。
初期掲載は本人提供の「自分をひと言で」「休日」「最近ハマっていること」の3問。判断・失敗・開発のきっかけ・相方についての4問は、回答を受け取ってから追加します。
正式な法人・団体名や個人情報の管理責任者は、運営メンバーの紹介や問い合わせ窓口とは別に確定する必要があります。

公開写真は `public/team/oya.jpg` と `public/team/takeuchi.jpg`。元写真の画素・画像サイズは変えず、EXIF/XMP・IPTC・コメントだけを除去しています。
`scripts/import-team-photo.mjs` は JPEG の画像本体を再圧縮せず、元写真とのデコード画素一致を確認してから保存します。元画像は変更せず、既存の公開写真への上書きも拒否します。
新規取り込みは `node scripts/import-team-photo.mjs /path/to/photo.jpg oya`（または `takeuchi`）。差し替え時は対象画像をバックアップしてから取り込み、`lib/team.ts` の縦横サイズと表示位置・画像テストも更新してください。
カード内だけCSSで写真の表示範囲を調整し、「写真を大きく見る」でメタデータを除いた全体画像を確認できます。

### 公式SNS・問い合わせ先

運営者が提供した公式SNSと公開問い合わせ先を `lib/site-contact.ts` に集約しています。変更時はこのファイルを更新してください。
Instagramは `https://www.instagram.com/oshisen.official/`、Xは `https://x.com/OshisenOfficial`、メールは `oshisen0914@gmail.com` です。Instagramの共有用クエリ `stkn` はリンクに含めません。
`/about#contact` とフッターから公式アカウント・問い合わせ先へ進めます。仕組み・情報源・プライバシーの説明にも同じメールアドレスを使用しています。

SNSは通常の外部リンクだけで、SDK・投稿の埋め込み・新しいクリック計測は追加していません。候補者のSNS、政策の一次情報、候補者用の操作記録とは区別しています。
問い合わせは `mailto:` とアドレスのコピーで対応し、サイト内フォーム・送信API・自動送信はありません。利用者自身がメールアプリで送信します。政策回答・保存候補などは本文や添付に自動追加しません。
受信メールの送信元アドレスや本文は返信・内容確認のために扱うことを `/privacy` に記載しています。法人・団体としての正式な運営主体、管理責任者、編集・訂正の確認体制は引き続き別途確定が必要です。

### 候補者・取材の本番掲載

現在、候補者は登録しません。取材や候補者データの取得が始まってから、次を確認します。

1. 対象者、掲載資格、氏名・選挙区等と一次情報を確認する。
2. 政策質問の確定版と補足説明を、候補者と利用者でそろえる。
3. 本人回答、理由・条件・参照資料、質問の版と尺度、確認状況を別々に保持する。
4. 共通6問の取材後、原音照合、発言区間、共通ブロック、引用・要約・条件を確認する。
5. 別担当者の文脈確認と本人による発言趣旨確認を分け、承認した版を公開する。
6. Podcastの該当時刻・全編・文字起こし・関連政策・公式資料への導線を接続する。
7. 訂正・追記と公開版の履歴を残す。未回答を中立や別資料の意見で埋めない。

既存の政策出典とリンク形式は [docs/policy-data.md](docs/policy-data.md) にあります。新しい照合・取材情報の扱いは [docs/research-design.md](docs/research-design.md) を優先してください。
CMS、認証付き編集、サーバー保存、公開承認の管理画面は未実装です。権限・保存先・確認体制を決めてから接続します。
研究については、主要評価・主比較・人数・終了基準・欠測と除外・分析方法・通常利用と分けた同意・管理責任者を、募集前に確定する必要があります。

サイトは準備中のため検索エンジンへのインデックスを無効にしています。
本番データを掲載して検索公開する際は、`app/layout.tsx` のrobots設定、`public/robots.txt`、`next.config.ts` の `X-Robots-Tag` を合わせて見直してください。
検索抑制はアクセス制限ではありません。Social preview の制作記録は `docs/social-preview.md` にあります。
